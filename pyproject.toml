[project]
name = "research-paper-pipeline"
version = "0.1.0"
description = "mdharness research-paper pipeline (research → draft → integrity → review → revise → finalize)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "mdharness @ git+https://github.com/tlorans/mdharness.git",
]

[tool.uv]
package = false
